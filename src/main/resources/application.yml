server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devdb
    username: postgres
    password: postgres
  application:
    name: sample
  jackson:
    time-zone: Asia/Kolkata
  jpa:
    hibernate:
      ddl-auto: update
      jdbc:
        time_zone: Asia/Kolkata
    show-sql: true